{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "V2 sprite index",
	"description": "Index of the curated sprite set shipped under data/v2/. Generated by tools/gen_v2_sprites.py from design/use_these_sprites — never hand-edited.",
	"type": "object",
	"required": ["version", "source", "sprites"],
	"additionalProperties": false,
	"properties": {
		"version": { "type": "integer", "minimum": 1 },
		"source": { "type": "string" },
		"sprites": {
			"type": "object",
			"minProperties": 350,
			"description": "key = family/name, matching data/v2/<key>.png",
			"propertyNames": { "pattern": "^(ui|icons)/[a-z0-9_]+(/[a-z0-9_]+)?$" },
			"additionalProperties": {
				"type": "object",
				"required": ["w", "h", "themed"],
				"additionalProperties": false,
				"properties": {
					"w": { "type": "integer", "minimum": 1 },
					"h": { "type": "integer", "minimum": 1 },
					"themed": {
						"type": "boolean",
						"description": "true = a <name>_mystic.png twin exists beside it, so the Mystic theme swaps art"
					},
					"mysticOnly": {
						"type": "boolean",
						"description": "true = the only art is the _mystic file; used in BOTH themes, flagged so we never claim it is vanilla"
					}
				}
			}
		}
	}
}
