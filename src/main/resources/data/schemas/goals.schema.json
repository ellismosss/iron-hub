{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Iron Hub goal targets",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "goals"],
  "properties": {
    "$schema": {"type": "string"},
    "version": {"type": "integer", "minimum": 1},
    "goals": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "name", "steps"],
        "properties": {
          "id": {"type": "string", "pattern": "^[a-z0-9_]+$"},
          "name": {"type": "string", "minLength": 1},
          "achieved": {
            "type": "array",
            "minItems": 1,
            "items": {"type": "string", "pattern": "^(skill|quest|item|unlock|kc):"}
          },
          "steps": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["label", "requirement"],
              "properties": {
                "label": {"type": "string", "minLength": 1},
                "requirement": {"type": "string", "minLength": 1}
              }
            }
          }
        }
      }
    }
  }
}
