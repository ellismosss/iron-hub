# Iron Hub — Claude Code Handoff

RuneLite Plugin Hub plugin: an all-in-one progression companion for standard OSRS Ironman accounts. Design phase is complete; your job is implementation.

**Before touching any feature that reads game data: read [DOMAIN-NOTES.md](DOMAIN-NOTES.md)** — OSRS domain knowledge (item identity layers, requirement semantics, detection limits) collected from the wiki after real in-client bugs. The gear pack is generated and wiki-audited via `tools/` (never hand-edit the JSON).

## Document map (read in this order)

1. **DESIGN.md** — full feature spec. §2 architecture, §3 the 21 feature modules, §7 roadmap phases.
1b. **ENGINE-DESIGN.md** — the goal-planner engine blueprint (IMPLEMENTED — `com.ironhub.engine`, E1–E5 complete): 7-layer architecture (projection→catalog→compiler→expander→cost→router→explainer), data-pack pipeline (methods/quests/sources/effects from WOM ironman EHP configs + wiki Lua modules + QH), E1–E5 implementation roadmap, and the July-2026 ironman meta research in Appendices A–D. Read before any engine/planner work.
1c. **design/PLANNER-UX.md** + **design/goal-planner-mockups.html** (frames 4a–4f) — the planner UX proposal over the engine: one plan / three views (Today · Route · Goals), reorder-by-constraint (pin/snooze/ban, never drag), no-silent-reshuffle update banner, explain cards, merge-preview add flow. IMPLEMENTED as `PlannerTab` (Goals view embeds the legacy 2d list). Open questions for Luke at the end of the doc.
2. **design/DESIGN-PACKAGE.md** — UI handoff from design: tokens, shared atoms, per-screen specs (frames 1a–3f), interaction rules. This is the UI source of truth.
3. **design/iron-hub-mockups.html** — open in a browser. High-fidelity mockups; frame id badges (1a–1d visual system/P0, 2a–2g module tabs, 3a–3f overlays/infoboxes) match the sections in DESIGN-PACKAGE.md.
4. DESIGN-HANDOVER.md — the original design brief; superseded by the design package where they differ.

## The Iron Hub way — patterns every new addition must follow

The design docs above define the product; these are the implementation patterns that emerged
building it. Every live module follows all of them — new work must too, or it will read as
foreign next to the rest.

**Adding (or rebuilding) a module — the recipe.**
1. Package `com.ironhub.modules.<name>`; a class implementing `IronHubModule` (`name()` = the
   nav-row label, `enabled()` = its `IronHubConfig` toggle, `startUp()`/`shutDown()` strictly
   symmetric: eventBus register/unregister, overlays + infoboxes add/remove, `tab.dispose()`).
   Register it in `IronHubPlugin.provideModules(...)`, add the config toggle
   (`modulesSection`), extend `ModuleLifecycleTest` (ctor call + module count) and confirm
   `PanelGalleryTest.navRowsExistForImplementedModules`.
2. Constructor-inject dependencies; every client-service param (`Client`, `ItemManager`,
   `OverlayManager`, `InfoBoxManager`, `Notifier`, `ClientThread`, `ConfigManager`) must
   tolerate null — headless tests construct modules with nulls and the module must still
   start, build its tab and render. Event handlers run on the client thread; tab rebuilds
   happen ONLY on the EDT, and a tab's state/plan listener is ALWAYS
   `RebuildGate.install(this, this::rebuild)` (2026-07-17 freeze audit): hidden tabs mark
   dirty and rebuild once when next shown, visible tabs coalesce bursts — a bare
   `invokeLater(this::rebuild)` listener rebuilds forever-invisible tabs on every state
   change, and with all hubs visited that was ~16 full rebuilds per kill. (Headless keeps
   always-rebuild semantics; build.gradle pins `java.awt.headless=true` for tests. A direct
   off-EDT rebuild races the listener and doubles every row; proven twice.)
3. Game state is read through `AccountState` (plus `watchVarbits`/`watchVarps`), never the
   Client directly from module logic — that is what keeps modules unit-testable. Re-render on
   relevant change only (fingerprint-compare before rebuilding), never per-tick. Unbounded
   row lists are forbidden — cap at ~50 with an honest "+ N more — refine your search" line
   (the Bank grammar; clog/CA adopted it after rendering hundreds of sprite rows per rebuild
   measured as a freeze contributor).
4. Persistence: add fields to `PersistedState` + accessors on `AccountState` that
   `persist(); notifyListeners();`, and wire BOTH `activateProfile` (load) and `persist()`
   (snapshot). Gson keeps unknown/removed JSON keys harmless; field initializers are the
   defaults for old profiles. `persist()` is COALESCED on live clients (dirty flag, tick
   flush ~3s; logout/hop/profile-switch/shutdown call `persistNow()` — it used to serialize
   the whole account on the game thread twice per kill) and synchronous headless; the
   ProfileStore executor owns both toJson and the write, fed a deep-copied snapshot.

**Static game content is a generated data pack, never Java constants.**
`tools/gen_<name>.py` → `src/main/resources/data/<name>.json` + a draft-07 schema under
`data/schemas/` (DataPackTest fails the build for any pack without one). Generator
conventions: pin the upstream source (hub commit / RuneLite tag / wiki page), descriptive
User-Agent with repo+email, cache under `tools/.cache-*/` (gitignored), fail fast on any
unresolved constant or name, cross-validate references at generation time (e.g. quest tokens
against quests.json), and end with sanity asserts on counts. Requirement strings inside packs
use the graph's `parse()` form — an unparseable string becomes a never-met manual gate, so a
pack test asserting every req parses non-manual is mandatory.

**The requirement graph is the only gating logic.** Never hand-roll met/unmet checks:
`Requirements.parse(...)`, `isMet(state)`, `describe()` for labels, `missing()` for unmet
leaves. Know the semantics before writing strings: `skill:` = hard/equip gate, `skillb:` =
boostable action gate, `item:` counts all ItemVariationMapping variants, `itemx:` = exact id
(tiers/imbues), `unlock:` keys must contain no colons, `any:a|b` with `&`-joined leaves =
alternative obtainment paths. DOMAIN-NOTES.md explains when each applies.

**"Track this" always means a synthetic Goal-planner goal** (standing user directive, applied
to gear, CA, diaries, collection log, clues, quests). Any track/target/+ affordance creates a
`<module>:<id>` goal through the UNIFIED seed system (Goals v2 G1, 2026-07-18): add a factory
method to `com.ironhub.state.GoalSeeds` (the family transform — finished name/step
labels/proofs built ONCE at add time so the goal renders offline) and call
`state.addGoalSeed(GoalSeeds.<family>(...))` / `state.removeGoalSeed(goalId)` — one persisted
`goalSeeds` map (`PersistedState.GoalSeed`: id/family/name/icon/steps/achieved/addedAt,
addedAt=0 means migrated-date-unknown), one `toGoal(seed)` in GoalPlannerModule, one loop in
`allGoals`, one generic branch in `removeGoal`; proof-marking modules scan
`state.goalSeedIds(family)`. The five legacy per-family maps (caGoals/diaryGoals/clueGoals/
clogGoals/customGoals) migrate in `activateProfile` via the same factories (write-through
once; GoalSeedMigrationTest pins byte-identical output). Achieved proof = a `<prefix>_<id>`
unlock flag the module marks from live data (and immediately at add time when already
satisfied). Requirements in the seed become honest planner steps. Never build a module-local
tracked list — dashboard picks planner goals up for free. **Goals v2 G2 added four more
providers (all four "+ Goal" affordances on existing tabs, no new modules):** PoH
(`poh:<tierId>` — proof `unlock:pohtier_<sanitized>`, tier ids carry a colon so the key MUST
`replace(':','_')` or the graph's colon-split unlock: parse truncates it — `GoalSeeds.pohProofKey`;
marked by PohModule's `markPohGoalProofs` listener), QoL (`qol:<id>` — achieved by OWNING the
unlock via `item:`/`any:` ownership proof, NOT "reqs met"; the prose reqs are display steps),
Bank (no new family — the skill-target `+ Goal` builds the SAME `custom:skill:<skill>:<lvl>`
id the planner search builds so they dedupe; capped 2..99 since `skill:` gates on real level),
Supplies (`supply:<itemId>` one-shot "stock N × item", proof `item:<id>:<N>:<name>`, re-addable).
**Completion archive (G2):** `PersistedState.GoalRecord` (goalId/name/family/addedAt/completedAt/
estimatedHours/hoursAtCompletion) in a capped-200 `goalRecords`, upserted by goalId; the
detector lives in `GoalPlannerModule.detectCompletions` (planner thread, in replanNow): a
SELECTED goal flipping unachieved→achieved records once, dated now() only if seen unachieved
this session (`seenActiveGoalIds`), else `completedAt=0`="detected"; the first pass never
re-records an already-archived goal (login replay keeps its date); removal never records.
`estimatedHours` captured from `previewMerge` at add time (0=unknown, module `+` clicks leave 0).

**Combining or porting other plugins — the playbook** (used for Log Adviser, Time Tracking,
Time Tracking Reminder, Easy Farming; Loadout Lab was imported whole only on explicit user
direction). Default is PORT, not import: fetch the source at the plugin-hub pinned commit
(`raw.githubusercontent.com/runelite/plugin-hub/master/plugins/<name>` → repo+commit) and
read it fully before writing anything. Keep their proven math, data tables and detection
tricks byte-faithful; replace their state/config/UI layers with AccountState, the requirement
graph and UiTokens. Convert their data to a generated pack. Vendored upstream CODE (when
package-private visibility forces it) is emitted by a generator with writes stripped, and
pinned by a reflective parity test against the classpath client so client updates fail CI
with a regenerate command (`TimetrackingParityTest`, `tools/gen_timetracking.py`). Reading
another plugin's config group read-only is hub-safe precedent; never write another plugin's
group. Always: `licenses/<name>-LICENSE`, class-header attribution, README credit (BSD-2
requires it).

**The polish bar — what "done" means here.**
- Full `./gradlew build` green before every commit; never commit or push red.
- Every new UI surface writes a render PNG from a test (`build/reports/*.png`) — and you must
  actually LOOK at the render before calling the work done; that habit has caught doubled
  rows, truncation, and spacing bugs the assertions missed.
- Honesty is a feature: unknown data renders as "?"/NaN/silence, never an invented name, rate
  or time; distinguish banked vs carried vs raw quantities explicitly; copy states its
  provenance ("as of last log open", "predicted"). Negative-path tests are expected
  (never notify on login replay, never flash Done on goal removal, unknown stays unknown).
- Tests use the shared fixtures (`StateFixture`, `TimetrackingFixture`,
  `InMemoryConfigManager` pattern: real logic over in-memory doubles, seeded byte-for-byte in
  the same format the live source writes) — not per-test mock forests. Derive game constants
  in tests by sweeping the real decoder/pack, never hardcoded magic values.
- UI: styles only from `UiTokens`; user-visible strings pass GlyphSafetyTest (ASCII plus
  `· … — ×`); 14 px text floor; semantic status colors never repurposed; detail that doesn't
  fit 225 px goes in tooltips; Swing specifics — `mousePressed` not `mouseClicked`, no nested
  scroll panes, explicit `LEFT_ALIGNMENT`, `getMaximumSize()` override on BoxLayout tabs, and
  UI-less custom JComponents MUST override `getMinimumSize()` (default = current size = 0×0
  pre-layout, and BoxLayout derives row alignment from child MINIMUMS — an all-zero-min row
  cuts children to half height top-anchored in the client, while SwingRender's multi-pass
  layout self-heals it; pin such bugs with a single-pass client-style mount test).
- Commits: one coherent slice per commit with an explanatory body, ending
  `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`; docs (CLAUDE.md state, README,
  DOMAIN-NOTES for any new game-data knowledge) updated in the same arc.

**Standing user gates (do not relitigate):** Plugin Hub submission happens only on the
user's explicit word — never prepare it unprompted (when they say go: repo needs a LICENSE
file and a hub-sandbox build check). External HTTP is opt-in or user-initiated only
(build-time generators are fine). Meta-data-pack regenerations (methods/rates refreshes,
the quarterly-ish cadence agreed for Goals v2) are NEVER implemented without first laying
the proposed changes out to Luke (2026-07-18). Bug reports get root-caused — verify pack data and the
upstream source before touching code; the reported symptom is often a data error. Match
reference plugins from their actual source, not from memory.

## Current code state

- `com.ironhub.IronHubPlugin` — entry point; a `@Provides Set<IronHubModule>` registers every implemented module (RuneLite ships Guice without the multibindings extension; `ModuleLifecycleTest` pins the count — currently 20). TODOs mark the rest: skills, boat. Config toggles gate module lifecycles (`enabled()` + ConfigChanged sync).
- `com.ironhub.IronHubConfig` — per-module enable toggles + opt-in integration settings.
- `com.ironhub.state.AccountState` — single-source-of-truth service: skill/quest/container ingestion, unlock flags, kill counts; profile-scoped persistence via `ProfileStore` (JSON under `RUNELITE_DIR/iron-hub/<accountHash>/`). Quest states refresh tick-throttled on varbit dirty (50 ticks — each refresh is ~210 QUEST_STATUS_GET clientscripts and the dirty flag trips on every varbit change). Test seam: `StateFixture` (test sources).
- **Performance architecture (2026-07-17 freeze audit — Luke: "clicking Bank freezes the dev client / it's been freezing a lot lately").** Root causes found and fixed, in impact order: (1) `persist()` Gson-serialized the entire account on the game thread with no debounce, twice per kill + once per bank slot event → coalesced (see recipe item 4); (2) every tab listener rebuilt on every state change, visible or not → `RebuildGate` (recipe item 2); (3) 210-clientscript quest re-poll every 6s → 30s; (4) paint layer: `OsrsIcons` now caches decoded PNGs (it re-read per call; SavedSetupView loads ghosts inside paint), `StoneBorder` pre-renders corner stamps per fill (was ~200 fillRect(1,1)/paint/box; DesignLab pixel pins prove identity), `BankRestockOverlay` resolves its supplier once per tick not per item-frame, `FarmingRunOverlay` memoizes missing-items + patch state per stop/tick; (5) clog/CA row caps. Diagnostics that stay: `EdtCostProbeTest` (seeded-account EDT probe, fails on order-of-magnitude storm regressions) and `EdtWatchdog` (`freezeWatchdog` toggle, default on — 1s EDT heartbeat, one WARN with EDT+Client stacks per stall episode, so any remaining freeze report arrives with a stack trace). The farming tracker's dirty refresh floors at 2 ticks (a timetracking config write near patches used to re-run the full patch sweep every tick).
- `com.ironhub.requirements` — the shared requirement graph (`Requirement` + `Requirements`): skill/skillb/quest/queststarted/item/itemx/unlock/kc/qp/diary + allOf/anyOf composites, `missing()` leaf resolution, and `parse()` for the data-pack string form (`skill:Farming:65`; `itemx:` = exact id, no variants; `any:a|b` = alternative obtainment paths, `&`-joined leaves; `skillb:` = boostable action gate); unparseable strings become manual (never auto-met) requirements. Temporary boosts: `Boosts.available(BoostsPack, state)` resolves usable per-skill headroom from `data/boosts.json` (each source gated on its own obtainment); `isMetWithBoosts()` consults it for `skillb:` leaves only — equip gates never boost. See DOMAIN-NOTES.md.
- `com.ironhub.data.DataPack` — bundled data-pack loader (`/data/<name>.json` → typed model, e.g. `DailiesPack`); content schema-validated in CI, loader fails fast on missing/corrupt packs.
- `com.ironhub.modules.*` — one package per module; tabs contributed via `IronHubModule.buildTab()`, routed by nav-row name (guarded by `PanelGalleryTest.navRowsExistForImplementedModules`). **Live (M3): quests, diaries, ca** — diaries rebuilt post-M8 to a built-in-journal-style interface: full 492-task catalog from `data/diaries.json` (`tools/gen_diaries.py` — wiki task text/requirements/rewards joined to Quest Helper's completion-flag mappings; varp bitfields per region, Karamja `ATJUN_*` varbits with count thresholds, four hand-verified reorder overrides — provenance + decode in DOMAIN-NOTES "Achievement diary task tracking"), region cards with four-segment tier bars (the THIN 5px variant since 2026-07-17, as is the overall StoneMeter; region names regular-weight, per Luke), single-expansion task lists showing only INCOMPLETE tasks by default (small-font "Show all"/"Hide completed" toggle; task + req text in the SMALL font — green done / amber = incomplete-but-doable via requirement-graph `skillb:`/`skill:`/`quest:`/`any:` leaves; display-only req lines never gate), collapsible per-diary Rewards; displayed counts come from the game's own `<REGION>_<TIER>_COUNT` varbits and a claimed/fully-counted tier proves its tasks — ironman-only alternate flags are invisible to the client (see DOMAIN-NOTES); per-task `+` adds a synthetic `diary:<varp>_<bit>` goal to the Goal planner (seed in `diaryGoals`, proof = `diarytask_<slug>` unlock marked by a state listener); `Requirements.questByName` is trailing-dot tolerant for wiki-sourced names; ca rebuilt post-M8 to Combat Achievements Tracker parity: full task catalog from cache enums/structs (CaCatalog; provenance + bitfield decode in DOMAIN-NOTES), All/Goals/Bosses views with search/tier/status/type/sort filters, expandable task rows whose `+` adds the task to the Goal planner as a synthetic `ca:<id>` goal (seed snapshot in `caGoals`, proof = `catask_<id>` unlock the module marks on catalog load), boss grid + drill-down, tier goal config (`caTierGoal` Auto/fixed) with goal-progress chat messages (`caGoalMessages`), community completion rates bundled from the wiki (`data/ca-completion.json`, `tools/gen_ca_completion.py`), wiki tier icons under `data/icons/ca/`. **Live (M4): bank — grown 2026-07-17 into the bank WORKBENCH: search-gated (nothing renders until a search, the stat filter, or a view is picked; every list caps at TOP 20), equipment-stat ranking dropdown, a "Highest alchs" view (Alchemiser semantics — HA price × quantity descending; per-row Exclude checkbox persisting to `PersistedState.alchExcluded`, "Reset excluded (N)" restores), a skill-icon strip (SkillIconManager is CLIENT-FREE, works headless) whose views rank owned items by total banked XP over the **Banked Experience data port** (tools/gen_banked_xp.py at hub pin 788fbbf → banked-xp.json v2, 641 entries/12 skills, optional `level` gate field; per-dose potions at xp×dose; PRAYER IS BASE RATES — upstream models altars as modifiers, so no gilded-altar rows; BSD-2, licenses/banked-experience-LICENSE), row multi-select whose items GLOW in the real bank (module-owned selection set + shared BankRestockOverlay; "Clear selection (N)"), and Item Stats-style hover tooltips (equipment bonuses + count + HA value). ALL per-item values (ItemEquipmentStats + getHaPrice) resolve in ONE client-thread sweep per bank snapshot, cached — the stat filter, alch view and tooltips read the same cache; BankTab.statOf maps the dropdown indices, BankTabTest pins the idle-lists-nothing state and the mapping. **Second pass same day:** alch rows in the SMALL font as "stack/per gp/ea"; 16px scaled sprites (raw 36×32 cropped); `BankCollectView` (FarmBankLayout generalised — hidden Bank Tag + Layout, row-major in ranking order, readable title on BANKMAIN_FINISHBUILDING) collects the sidebar's current set IN THE REAL BANK — the SKILL view's whole result list, else the selection; only selected items keep the green glow; module registers to the EventBus for BANKMAIN_INIT re-assert. Pack v3: entries carry the upstream `activity` CONSTANT (display names collide — modifiers join on it), declarative `secondaries` (fractional qtys preserved), `outputId/outputQty`, and a 9-modifier table (Wildy Altar emitted at effective ×7.0 = 3.5xp × 50% save; Zealot's robes + 14 custom-handler secondaries skipped with logging — see gen_banked_xp.py). The SKILL view is the full Banked Experience interface: header card (level now / banked xp / level banked / xp to next via `net.runelite.api.Experience`), modifier checkboxes reshaping totals live, per-item activity selector (session-scoped choice; a row's xp VALUE expands the detail, the rest of the row selects), secondaries have/need lines coloured by sufficiency; totals count every owned item. Render: bank-skill-view.png; pinned: 1,000 d-bones 72K → 252K with the altar lit. **Third pass (fec538c):** the icon strip IS the switchboard — High Alchemy wiki icon (data/icons/osrs/high_alchemy.png, native size) toggles the alch view, five combat stat-group icons (best-bonus-in-group ranking via `BankTab.groupValue`), and Runecraft (essence counts w/ honest zeros, level-gated Regular-action selector from xp-actions.json, Daeyalt line; essence ids pinned vs the name index). Snapshot line DELETED; alch sorts Stack/Each (chip) with both gp figures RS-truncated; visible aggregated Secondaries section (have/need, ceil of qty×count sums); ALL result rows small font with muted ×qty before the figure; persisted per-skill target level (`bankSkillTargets`) + thin meter. **Fourth pass (e92cdf0, 24 items):** strip divider + skill icons on their own rows; alch Each-default, qty-1 rows gp/ea-only, resting grey-x exclude box, QUANTITY-AWARE exclusions (`alchExcludedAtQty` map — excluded at 1 auto-returns when owned >1 and clears, re-exclude then permanent; legacy Set migrates as permanent); skill header "Name [bold] — L [reg] (B banked) [green]", orange Next-level, "Target level:" auto-filled to first unbanked level (only user edits persist), levels flank the red→green lerped meter, centered "N xp banked/selected"; modifier greying by selection's chosen activities + EXCLUSIVITY ported from upstream compatibleWith (pack v4 `exclusiveGroup` "prayer-exclusive" = Sinister/Gilded/Ecto/Wildy; generator asserts the clique); selection grammar = plain exclusive / cmd-ctrl toggle / shift range (`BankTab.applyClick` pure); single-selection detail card IS the activity chooser (value-click expansion deleted); secondaries grey when inapplicable + ride the bank collect; ×1 never renders; item-stats card + 2-column cycling Compare; Banked XP summary = always-grid slab XpTiles (icon, cur→banked painted arrow, per-level thin meter; bankedXpGridView config DELETED)**, qol (requirement-graph checklist), dailies (rebuilt post-M8 as a guided run — see below)**. **Live (M5): gear (rebuilt post-M8 as a Ladlor-style progression chart per user direction, superseding frame 2a: `gear-progression.json` pack — phases/groups/items generated from ItemID constants, 180 entries from the Complete Account Progression Guide; sprite tiles via ItemManager.getImage, green=owned via canonicalStock, orange=targeted, left-click targets into the goal planner as synthetic `gear:<slug>` goals via GoalPlannerModule.toGoal/allGoals, right-click wiki + manual mark for POH entries (`gearmark_<slug>` unlock — no colons, keys round-trip through `unlock:` parse), hover=missing reqs, filter chips melee/ranged/magic/utility/poh/boat, hide-complete toggle (persisted `gearHideComplete`), ready-triangle when reqs met, right-click Mark-as-obtained on every entry (mark auto-untargets); `implies` chains: owning a successor proves its consumed/superseded predecessors (assembler→accumulator→attractor, treads→the three boots, slayer helm→black mask — fixpoint in `GearProgressionModule.obtainedNames`; add `implies` when adding upgrade-path entries); AccountState.setUnlocked notifies listeners (manual marks/ticks re-render immediately); tiered/imbued entries carry `exact:true` → `itemx:` exact-id requirements (variation groups span diary/hilt tiers — canonical matching false-positives them); goal rows/cards show item sprites via `GoalsPack.Goal.icon()`; HubScrollPane view is width-tracking Scrollable (plain anchor let wide WrapLayout rows overflow 225px); gear-ladders.json retained for the dashboard upgrades strip), loadout (DELETED 2026-07-16 in the nav rework, superseded by Loadout Lab; was: top-level module per user direction — deviates from 2f's embedded placement; rebuilt post-M8 as a task-aware planner: live equipped+inventory grids from AccountState slot arrays (`getEquipmentSlots`/`getInventorySlots`; equipment notifies immediately, inventory tick-throttled), activity detection (slayer task pushed via `AccountState.setSlayerTask`, fought NPC via InteractingChanged→`setCombatTarget`), user-initiated wiki strategy fetch (`StrategyClient`→`WikiStrategy` parser for {{Recommended equipment}} templates + tabber styles + preceding-prose tips, fixture-tested against captured Dust devils/Zulrah wikitext), `ItemNameIndex` offline name→id (tools/gen_item_names.py; apostrophes vanish in constants), `StrategySolver` best-owned-per-slot (rank order, owned-variant resolution, 2h clears shield), click-to-swap popups (overrides win, NEXT-state tint), saved setups per activity (persisted `savedLoadouts`), DPS export with target monster (calc rehydrates by id — verified in their state.tsx), Inventory Setups clipboard export (portable {setup,layout} format verified from their serialization sources); BETA heuristic solver remains the no-strategy fallback), loot (per-source totals + per-kill view over NpcLootReceived)**. Remaining stubs: skills, boat, farming, goals, whatnow, clues, slayer, supplies, collectionlog, sync, dashboard-module, death. **M5 complete (v0.3): + dps export (protocol verified live), loot (2g incl. supplies-used), slayer basics (varps + cache-DB task names + infobox)**. Supplies consumption = trip diffing: checkpoint carried gear at bank interactions/kills, decreases attribute to the killed source, potion sips cancel via ItemVariationMapping. Still open for later milestones: uniques pack, per-hour/session views, block/skip advisor. **M6 in progress: farming** — run timer + proximity-based patch checklist (2e tab, 3b overlay, run-timer infobox), first Overlay + first ShortestPathBridge consumer; crop-stage varbits pending. **death recovery** (ActorDeath capture: when/where/carried, capped history, Path to spot; reclaim costs later). **herb crop states**: HerbPatchDecoder generated from core's PatchImplementation.HERB at the pinned client tag, held equivalent by a reflective parity sweep (HerbDecoderParityTest) — regenerate from the tag if it fails. Last-seen patch states persist (farming varbits only sync in-region); readiness predicted at 20 min/stage; patches-ready infobox hidden at 0. Notifications: patch-ready + daily-reset via Notifier, per-category config toggles. M6 code-complete except the boat pack (parked until Sailing content is stable). **M7 complete (v0.5): goals (2d checklist-first; steps auto-complete via the graph, manual steps tick through unlock flags; achieved-by-ownership: per-goal `achieved` item proofs retire done goals to a Completed section — covers pre-plugin accomplishments KC/manual steps can't detect; `item:` requirements count all ItemVariationMapping variants, e.g. recoloured graceful), supplies runway (rolling consumption log rates), whatnow (impact x urgency x fit over cross-module statics — DELETED 2026-07-16 in the nav rework), clues (emote readiness from starter pack; STASH deferred — no API constants)**. **Post-M8: the goal ENGINE (`com.ironhub.engine`, per ENGINE-DESIGN.md E1–E5, implemented): StateView read-interface (AccountState + ProjectedState both satisfy it — the requirement graph evaluates against live or simulated accounts), GoalExpander (goals → merged deduped action DAG: quest chains from quests.json, items via the gear pack, per-level TRAIN nodes chained so a 25-Ranged quest never waits on a 60 demand, qp: fills from the route order, any: picks cheapest path, unparseable → MANUAL tick nodes on the goals module's own goalstep: keys), Router (greedy value-density over forward simulation — quest xp/travel effects genuinely re-cost later steps; forfeit penalty defers grinds whose xp pending quests will pay; pins jump with prerequisites, snoozes sink, bans/preferred flow into CostModel; OQG route prior breaks ties; deterministic byte-for-byte; NaN durations honest, never invented), PlannerService front door + debounced off-thread replans in GoalPlannerModule (400ms, engineActive lifecycle guard, static sharedPlan seam consumed by WhatNow's top card). Packs: quests.json (tools/gen_quests.py: OQG/Ironman order+xp+qp joined with per-quest infobox reqs/lengths; 198 quests all enum-resolved), methods.json (tools/gen_methods.py curated 24-skill seed, WOM-shape {startXp,rate} + req overlay + bonus edges; WOM = test envelope only; every ladder needs a level-1 floor method or plans stall — see RouterTest), effects.json (travel-factor discounts; queststarted:/qp: are graph leaves added for these). UI: PlannerTab (design/PLANNER-UX.md frames 4a–4f): Today (budget chips incl. AFK, accent NOW card with computed why, up-next, plan-aware passive lane, since-last-session strip vs persisted lastPlanHours), Route (per-run chaptered steps, no-silent-reshuffle update banner with apply + hour delta, explain-card expand with alternatives/prefer + Pin/Snooze/Ban, right-click menu, SNOOZED sink section), Goals (add-goal search incl. "skill level" parse + live dry-run merge preview via GoalPlannerModule.previewMerge, legacy GoalsTab embedded beneath). Constraints persist profile-scoped (plannerPins/Snoozes/Bans/Preferred). Renders: build/reports/planner-{today,route,goals}.png. Canvas companion: `PlannerOverlay` (frame 3c grown over the live engine, `plannerOverlay` toggle) — follows the plan head: XpGauge mirrors RuneLite's XP Tracker math verified upstream (wall-clock accrues from first drop so xp/hr DECAYS while idle, 60s elapsed floor, 10-min idle resets the rate window but total gained survives, actions-left = their rolling mean of last 10 action xps); xp-left/TTL use the RAW gap to the target level, never trainXpRemaining (banked materials save gathering, not training time — banked-aware "0 left" confused in-client); 14px sub-pixel eased bar with 2dp % label anchored on LIVE xp per step id (banking must never reset the bar); method line names reality: curated methods.json xpEach×k match first, else the wiki-calculator fingerprint from `data/xp-actions.json` (tools/gen_xp_actions.py — 18 Module:Skill calc Lua modules, 2007 per-action xp values incl. agility laps; ties sharing a base name show the base "Maple longbow", genuine ambiguity = null), else >30% pace divergence = "Your method" — never an invented name; missing resources, Next + Goal footers, green "Done ·" flash only when the vanished head is genuinely satisfied (goal removal never flashes), right-click Snooze step / Mark done (manual heads only) via Overlay.addMenuEntry callbacks; render = build/reports/planner-overlay.png.** Exit criterion unit-proven in GoalPlannerTest. **M8 complete (v1.0): collectionlog (title sync on log open + KC list; rebuilt post-M8 — see below), live dashboard (AccountScore blend, clickable components, daily snapshots + sparkline/trend, live WhatNow/goal/alerts/upgrades), sync (WOM/Temple logout ping + Discord level/goal webhooks — all opt-in, documented in README for Hub review)**. Remaining stubs: skills (+ unregistered boat, parked). DashboardModule stays tab-less (dashboard IS the panel home). **Imported: Loadout Lab** (per user /goal): upstream `com.loadoutlab.*` copied verbatim at hub commit 55587ac (BSD-2, licenses/ retained; their 53-file test suite runs in our build — JUnit5 platform + vintage + mockito added) with three decoupling edits in LoadoutLabPlugin (public lifecycle, NavigationButton block → panelReadyCallback hook, navButton removal dropped); `com.ironhub.modules.loadoutlab.LoadoutLabModule` wraps it (eventBus.register(lab) drives its 8 subscribers, panel mounts as the 'Loadout Lab' tab via an async-safe holder), `loadoutLab` config toggle, `@Provides LoadoutLabConfig` in IronHubPlugin (imported @Provides isn't a Guice module). **Import lesson: @PluginDependency must move to IronHubPlugin** — the imported class's own annotation is inert; BankTagsService/TagManager only bind when the REGISTERED plugin declares the dependency (missing it = Guice CreationException = Iron Hub silently absent from the sidebar). Data ships at /com/loadoutlab/data (~460KB gz). Wrapper one-stop-shop layer (all in LoadoutLabModule, upstream untouched): auto-follows slayer task / last fought+killed NPC via panel.selectExternal (kills also update AccountState combat target; `labFollowActivity` toggle), user-initiated wiki tips via the shared StrategyClient (now public), and per-activity SavedSetup persistence (PersistedState.SavedSetup: worn gear + inventory slots/qty + rune pouch via Varbits.RUNE_POUCH_* + EnumID.RUNEPOUCH_RUNE on the client thread) rendered as the GAME'S OWN interfaces since 2026-07-17 (SavedSetupView): the Worn Equipment layout with real slot sprites + chain links (slot positions template-matched in the wiki's native-1x Worn_Equipment_tab.png; links cropped from it — packs don't redraw links so both themes share them; vanilla slot sprites = Luke's drop at data/icons/osrs/equipment/, Mystic = the pack's at mystic/equipment/), placeholder silhouettes on empty slots, and the inventory as items directly on the framed side-panel backing (Mystic: bundled fixed_mode/side_panel_background; vanilla: SpriteID.RS2_SIDE_PANEL_BACKGROUND from the sprite cache at runtime via the newly injected SpriteManager, honest recess fallback headless). OsrsIcons.image() is the themed BufferedImage lookup for custom painting. **Post-M8: collectionlog rebuilt to Log Adviser parity** (per user /goal; ported, not imported — BSD-2, licenses/log-adviser-LICENSE): `data/clog.json` (tools/gen_clog.py from the pinned Log Adviser hub commit — 256 activities with IRON completions/hr + 2,522 drop rows in four rate-mode flags, 1,706 slots, body-type aliases + chat-only names; activity reqs converted to graph strings, every quest token validated against quests.json at generation; zero-attempt rows = purchase slots, never rate-ranked); `ClogRanker` pure TTNS math (harmonic Neither bucket / Independent / Exact / E&I minimums, requires-previous chains, unlocked-first sort with locked demoted but ordered, missing labels via the requirement graph); sync brain in CollectionLogModule (chat "New item added" drops incl. alias resolution, passive script-4100 harvest while browsing, LogSyncButton 9-slice port on the log header — steps left if Log Adviser's own button is present — pressing the game Search + enumerate script 2240, COLLECTION_COUNT-vs-baseline drift nudge; scripts + varp semantics in DOMAIN-NOTES "Collection log detection"); tab in the CA grammar (progress+sync card, search across activities AND items, Show filter incl. the Slayer-req rule, top-30 rows with item sprites/TTNS/next-slot line + Show all, expansion = per-slot owned ticks + 1/x rates, right-click wiki/skip, SKIPPED sink); every slot's +/× = synthetic `clog:<itemId>` goal (seed in `clogGoals` snapshots name/activity/reqs so activity gates route honestly in the planner; proof = `clogitem_<id>` unlock the module marks on obtain, immediate when already owned); render = build/reports/collectionlog-tab.png. **Post-M8: farming rebuilt as the FARM RUNS module** (per user /goal combining core Time Tracking + Time Tracking Reminder + Easy Farming): `com.ironhub.modules.farming.rl` = the core timetracking farming+hunter engine VENDORED read-only by `tools/gen_timetracking.py` at the RuneLite tag matching the client dependency (update.js-recipe port of the reminder plugin: package rename, visibility widening, EVERY config write stripped — the default-on core Time Tracking plugin is the single writer of the `timetracking` RSProfile group and Iron Hub only reads/predicts; format + rationale in DOMAIN-NOTES "Farming time-tracking"; TimetrackingParityTest reflectively sweeps the classpath client and fails CI with the regenerate command when decoding/world/varps drift; Tab regrouped one-category-per-type + hand-maintained trimmed FarmingTracker, both BSD-2 attributed); `FarmTrackingService` exposes per-category summaries/ETAs/harvestable, whole-world ready counts (static `FarmingRunModule.sharedReadyPatches()` consumed by WhatNow "Farm run" + dashboard), bird-house 50-min windows, contract tri-state, `coreTrackingDisabled()`/`hasAnyData()` honesty hints; refresh = throttled GameTick + instant on `ConfigChanged` group timetracking; per-category render-gated ready infoboxes (reminder parity, `farmingReadyInfoboxes` toggle) + once-per-transition notifications that never replay on login; `data/farm-runs.json` (tools/gen_farm_runs.py from the pinned Easy Farming hub commit, licenses/easy-farming-LICENSE — 29 stops herb/tree/fruit/hops in route order, 148 teleports with gameval-resolved item reqs; suppliers house/fairyRing = access-based; patch-types-at-stop derived LIVE from the vendored FarmingWorld by region because Easy Farming's own labels overclaim — FarmRunsPackTest proves every stop tile sits in a region with its patch type); run engine = template runs per category + custom runs persisted (`farmRuns` name→ordered ids, compact checkbox builder keeps pack route order), teleport AUTO-PICKED from ownership (bank counts for planning via `plannedStock`; carried-only variation-aware `missingItems` drives the red overlay line; ownership counts the RUNE POUCH — `AccountState.getRunePouch()` decodes the pouch varbits/enum on the client thread — and higher teleport tiers via `TELEPORT_UPGRADES`, so an enhanced/perfected quetzal whistle covers a basic one the pack asks for, and pouched law runes stop reading as missing), proximity advance routes ShortestPathBridge to the next stop; overlay = current stop + teleport + missing items + live patch states (UNKNOWN silent) + checklist (display-only, no menu); renders = build/reports/farming-{tab,run-active,run-overlay}.png. The **End run** control is pinned to the TOP of the tab during a run (rebuildTopBar: accent button + "<run> · N/M stops"), not buried under the patch overview — the sidebar is the surface, not an overlay right-click. Old HerbPatchDecoder/herb-patches.json/herbPatchSeen persistence deleted. Swing lesson re-proven: never rebuild a tab off the EDT (test raced the listener rebuild → doubled rows). **Bank setup (Inventory Setups port, BSD-2, licenses/inventory-setups-LICENSE):** a run carries an optional saved gear+inventory setup (`AccountState.captureSetup` snapshots worn slots + inventory ids/qty — rune pouch skipped, farm teleports are tabs/jewellery/loose runes; persisted per run in `farmRunSetups`, dropped with the run; saved from the active-run view). When a run with a setup is active and the bank builds (`onScriptPreFired(ScriptID.BANKMAIN_INIT)`), `FarmBankLayout` **reorganises the real bank** the way Inventory Setups does — a HIDDEN Bank Tag + `Layout` pinning each setup item to its bank-grid position (equipment in the preset worn-items arrangement HEAD=1/CAPE=8/…/RING=34, inventory in the right 4 columns), opened via `BankTagsService.openBankTag(tag, OPTION_HIDE_TAG_NAME)` so the bank filters + lays itself out; cleared (closeBankTag + removeTag + removeLayout, all client-thread) on run end / bank-open-without-run / shutDown. Uses the `@PluginDependency(BankTagsPlugin.class)` already on IronHubPlugin (declared for Loadout Lab) + injected BankTagsService/TagManager/LayoutManager. NOT a floating canvas overlay (a first attempt drew a grid on the canvas but left the bank untouched — wrong; the whole point is the bank rearranges). `farmBankSetup` toggle. **Lesson (2026-07-15, from Luke): when he reports an issue, take his words as fact and fix THAT — don't reinterpret. "No End run button" meant "hard to find → reposition it", not "add one to the overlay"; the overlay guess wasted a round.** **Post-M8 farm-run refinements (2026-07-15, dense feedback list):** overlay decluttered to the run's OWN category patch only (`FarmingRunOverlay.patchState` filters to `runCategoryTab`; the old all-categories line wrapped in the narrow panel and hid stops — that was why Troll Stronghold "wasn't showing") + a progress line with Farming xp gained and grimy-herb count (`GRIMY_HERBS`, baselined at run start); per-location **preferred teleport** (Easy Farming parity — `farmTeleportPrefs` location→teleport id, chosen in a collapsible "Teleport preferences" tab section, honoured by `pickTeleport` over the owned-first auto-pick even if unowned); **patch gating** — each farm-runs.json location carries `reqs` (curated from the wiki, quest tokens validated vs the Quest enum at generation; Harmony Island = `diary:Morytania:Elite` — Luke was right, NOT a quest), a locked patch never enters a run (`isUnlocked` via the requirement graph); **progression rewrite** — proximity marking (which advanced on arrival AND could skip a patch you passed near, desyncing the overlay from Shortest Path) REPLACED by Easy Farming's signal: a stop advances only once its seed is planted (patch growing via FarmTrackingService) AND composted — for EVERY category incl. trees/fruit (`COMPOST_APPLIED` chat pattern from the vendored CompostTracker, region-attributed via `onCompostApplied`). The compost is the LIVE "worked this one" signal; gating trees/fruit on the persisted GROWING varbit alone auto-advanced patches still growing from a PREVIOUS run, so a fresh run started half-done and cascaded to complete (fixed 2026-07-15 — every category waits for compost, Easy Farming parity; pay-for-protection tree runs use the manual skip). `nextStop()` drives BOTH overlay and path bridge so they can't disagree, re-routed on each advance; sidebar stop = click-to-`markThrough` (manual skip/escape hatch). No more proximity/`VISIT_RADIUS`. **Combined "Tree & fruit run"** (per Luke — most players do them together): `TEMPLATES` maps a name → one-or-more categories; `templateLocations` builds a combined run grouping co-located patches (Farming Guild + Gnome Stronghold have both — the fruit stop slots in right after its tree twin by region match, then fruit-only sites follow). Per-stop logic (advance gate, compost requirement, overlay patch state) keys on each STOP's own category (`categoryTab(stop.location.category)`), NOT a single run category — so a mixed run works; `multiCategory()`/`stopLabel()` suffix mixed-run stops with their type ("Farming Guild · fruit tree"). runCategory field removed. **All trees run + dynamic culling (2026-07-15, Luke's spec):** a curated **All trees run** (regular + fruit + calquat + celastrus, 18 stops in Luke's exact order — "Start at GE" deferred to the banking-steps spec) lives in a new `farm-runs.json` `routes` section (name→ordered ids; `FarmRunsPack.route`, resolved by `runLocations`/`templateNames`, picked in the tab alongside the category templates); it needs new categories **calquat/celastrus** (categoryTab→Tab.CALQUAT/CELASTRUS, stopLabel types, schema enum + id pattern widened) and curated locations gen_farm_runs.py adds after Easy Farming's tables — Farming Guild celastrus + Tai Bwo Wannai/Kastori/**The Great Conch** ("Summer Shore" per Luke, instanced) calquats, tiles placed in the vendored FarmingWorld PRIMARY region so live state resolves (FarmRunsPackTest proves it; guild tree/fruit/celastrus all = region 4922). Easy Farming's "Nemus Retreat" tree = the game's **Auburnvale** (same region 5427) — renamed via NAME_OVERRIDE at generation (keeps its richer teleports), NOT a curated duplicate. **`cull()` (at every startRun)** trims a run to what's worth doing: drops locked stops, stops whose own-category patch is `confirmedGrowing` (the predictor's plain GROWING view — nothing to harvest yet; ready/empty/dead/unknown = "assume doable"), and stops with no owned **sapling** (FarmRunsPack.saplings pack section, gen from client ItemID; short on a type → keep only that many stops in route order = "cull down to sapling count"; herb/hops have no sapling list → never sapling-culled). The overlay caps its upcoming list to MAX_UPCOMING (+"…+N more") so an 18-stop run stays in the 200px budget; the sidebar tab keeps the full checklist. render = build/reports/farming-run-combo-overlay.png. **Full wiki run lineup (2026-07-16, from oldschool.runescape.wiki/w/Farming_runs):** Herb/Tree/Fruit tree reworked from category auto-grouping to curated `routes` in wiki order; added Hardwood tree (category `hardwood`, teak+mahogany saplings; Fossil Island/Locus Oasis/Anglers' Retreat), Hop and bush (category `bush`; hop+bush patches are at DIFFERENT sites so it's interleaved single-category stops, no multi-patch stop needed), Allotment/flower/herb (co-located patches = SEPARATE stops per area — gen_farm_runs.py `companions()` copies each herb area's tile+teleports into allotment/<area>+flower/<area>; Prifddinas omitted — instanced region 13151 is off-map), and Supercompost (compost-bin checklist: `compost` category, `categoryTab`=null → never crop/sapling-culled, never auto-advances, manual markThrough only). New categories added to categoryTab/stopLabel/schema/FarmRunsPackTest CATEGORY_IMPLEMENTATIONS; every companion/curated tile proven to sit in a vendored-world region with its patch. TEMPLATES now holds only the non-wiki auto-group runs (Tree & fruit — redundant with Combo, candidate for removal; Hops). Modelling debt: co-located multi-patch stops are granular (3 rows per AFH area) — a future "multi-category stop" would collapse them; the wiki presents one stop per location. **UX polish pass (2026-07-16, Luke live feedback):** runs renamed/reordered (Combo tree run→"All trees run", picker order driven by pack `routes` dict order, then TEMPLATES; "Tree & fruit run" deleted); **sidebar switches to the active-run view immediately** on start (startRun queues `SwingUtilities.invokeLater(tab::rebuild)` itself BEFORE the Shortest Path `routeToNext` post — that post on the EDT can throw and skip the caller's rebuild, which left the picker up until the next bank open; routeToNext now try/caught); each run stop has a **Skip** button (IconButton.skip, wider than the 18px square) doing markThrough instead of the Shortest Path flag (tab no longer takes pathBridge); **teleport prefs grouped by place** (one row per location name, sets the pref for all co-located ids that offer the teleport); **patch overview = Time Tracking's exact interface** (`overviewByCategory()` → a strip of clickable category icon tiles; clicking a tile TOGGLES that category's patch list open/closed — nothing expanded = just the strip, minimal; only categories with a seen patch get a tile, but an expanded category lists EVERY patch incl. unknown ones. Per-patch rows were RuneLite's own `TimeablePanel` clone until 2026-07-17 evening (Luke: bring them to the design system) — now skinned rows (sprite · name · estimate over a thin StoneMeter) keeping the tracker's exact SEMANTICS: fill = `cropState.getColor().darker()`, value = stage/(stages-1), no bar for unknown patches or fully-grown weeds, "Done" green, estimate text otherwise faint. FarmingTab now takes ItemManager for the tile/produce sprites; bird houses/contract stay summary rows below. `expandOverview(Tab)` test seam. Rebuilt from an earlier per-patch-cards version Luke rejected as "every patch listed at once" with off colours). **Overview refinements (2026-07-16):** grown-weeds patches hide the bar (`weeds && stage>=stages-1`, Time Tracking parity); tiles MERGE via `displayGroup(Tab)` — Calquat/Celastrus → Tree tile, Hespori/Cactus/Belladonna/Mushroom → Special tile (expanded merged tiles sub-label by source tab; OverviewPatch carries `sourceTab`); each tile is a custom-painted `OverviewTile` — 1px GREEN border (ColorScheme.PROGRESS_COMPLETE_COLOR.darker()) when every seen patch is done, else a base border + ORANGE clockwise perimeter arc (ColorScheme.PROGRESS_INPROGRESS_COLOR) filling to the soonest-ready patch's stage/(stages-1) — expansion shown by background fill, not the border. **A grown patch reads GROWING with a PAST doneEstimate until checked ("Done") — count that as done, not 100%-growing, or every ready tile paints a full orange perimeter (the bug Luke hit).** Tiles whose patches are ALL weeds (nothing planted) paint no border at all. Expansion is SINGLE — clicking a tile closes any other; clicking the open one closes it (`expandedOverview` is one nullable Tab). Hovering a patch row tooltips what's planted ("Falador — Ranarr weed" / "Nothing planted" / "Unknown state"). Run picker order = **ready-first stable partition over the player's own order** (2026-07-16 third pass, Luke's synthesis: stacked up/down arrows per row persist `farmRunOrder`, ready runs lift to the top WITHIN that order — an arrow press across the ready boundary is a visual no-op, the sort owns that line; `runReady(name)` = any of the run's OWN stops with work waiting — a patch view that is known and not GROWING, so empty/dead patches to (re)plant count, not just harvestable (Luke's hardwoods were empty and read not-ready under category-harvestable; unknown stays silent) — plus bird houses done/empty and `contractReady()`), a run BECOMING ready **re-ticks itself** into Start all runs (`reTickReadyRuns`, transition-edged with a silent first-refresh seed like the notifications — an untick means "not this time", never replayed on login), and each row is arrows + checkbox (natural width — pinning it to 16px squashed the LAF's check icon) + icon + name + "Ready" (2026-07-16, Luke): icon from `RUN_ICONS` (run name → Tab → getItemID(); STATED not derived — "All trees" is a 7-tree/7-fruit tie whose first stop is a fruit tree — with `iconTab()` covering birdhouse/compost, which `categoryTab()` must keep returning null for since that null is load-bearing; custom runs fall back to their first stop; FarmingRunModuleTest pins every route name has one). Each row has a **checkbox** (`farmRunChoice`, absent = in) and a **Start all runs** button above the list starts every ticked run as ONE sequence (`selectedRunLocations()` — picker order, each stop once since runs overlap heavily, then the normal cull; run name "All runs"). The Bird houses status row was CUT (2026-07-16 second pass — it doubled the Birdhouse run row, whose `runReady` already reads the bird-house summary) and the **farming contract became a startable one-stop run** (third pass, Luke: `contract/farming-guild` curated in gen_farm_runs.py at the guild allotments tile with `copy: herb/farming-guild` teleports + route "Farming contract"; new `contract` category in the schema enum AND both id patterns — the enum is easy to miss; categoryTab null = manual markThrough, `iconTab`/RUN_ICONS → Tab.SPECIAL; cull drops the stop while `hasContract && !contractReady()` (crop growing) but KEEPS it with no contract at all — the run is how you go get one from Jane; `startRun` now refuses an empty post-cull run; fixture seeds a contract via the `contract` RSProfile key = the Produce item id). **Superset runs supersede (2026-07-16, revised same day):** a STATED map (`SUPERSEDES` — All trees → Tree + Fruit tree, Hop & bush → Hops) greys the smaller run's checkbox and drops it from "Start all runs" while the big one is ticked — derived, never persisted, so unticking the big run restores the small one's own stored choice. The map is ONLY for genuine stop-set containment, pinned by a containment test: Luke first asked for AFH → Herb too, then reversed when told AFH's wiki route omits Troll Stronghold/Weiss — every herb stop rides whenever the Herb run is checked, dedup absorbs the overlap. **Run-type bank setups (2026-07-16, Luke; per-stop switch same day):** collapsible "Gear & inventory" section under Teleport preferences ("Configure gear & inventory" clips past 225px) with six capture buttons — Trees (tree/fruit/calquat/celastrus) / Hardwoods / Herbs (herb/allotment/flower) / Birdhouses / Hespori / Others — saving `state.captureSetup()` under `farmRunSetups` key `type:<bucket>` (prefixed so a custom run named "Trees" can't collide); green text = saved; `activeSetup()` = the run's own setup else **the CURRENT stop's bucket** (NOT a whole-run majority — Luke: a combined run must serve the Trees loadout at a tree stop and Herbs at the herb stop after it; the bank re-applies per open and `FarmBankLayout.apply` keys on a content signature, not the run name, so the switch just works), feeding both the bank layout apply and the green highlight; the combined "All runs" sequence NEVER has a run-level setup (button hidden, `activeSetup` skips it — only per-stop buckets make sense for every-run-at-once); setup buttons flash "Saved" 1.5s + tooltip the saved item count (a click on an already-green button otherwise looks like nothing happened); a multi-line html hint needs `<div style='width:180px'>` or BoxLayout sizes it one line tall and clips. **Restock glow is bank-only** (`com.ironhub.ui.components.BankRestockOverlay`, used by farm runs AND dailies): Loadout Lab's `BankHighlightOverlay` also registers `showOnInventory()` — right for its gear sets, wrong for a restock aid, where the glow followed withdrawn items into the inventory and outlived the bank interface (Luke's report). **Run xp stats (2026-07-16, Luke):** every COMPLETED run persists a `FarmRunRecord` (capped 100) with Farming xp attributed per stop-type bucket as stops advance (`attributeXpTo` — the delta since the last advance goes to the stop just worked, so a combined run's record separates tree xp from herb xp) plus grimy herbs picked (id→count, inventory diff); sidebar rows "Tree runs · 12.4k xp/run" + "Farming 76 in ~11 runs: <first unlock> …" (tooltip = all unlocks) and the herb twin using POTENTIAL Herblore xp — pack `herbs` table (gen_farm_runs.py `HERB_POTENTIAL`: each grimy herb valued at cleaning + its signature potion, both xp values JOINED from xp-actions.json at generation, never typed; toadflax counts its Saradomin brew) — toward the next Herblore level; NaN average = rows absent, never an invented rate; new `data/skill-unlocks.json` (tools/gen_skill_unlocks.py — wiki `<Skill>/Level_up_table` raw wikitext, {{plink}}-stripped, Farming+Herblore only, anchored by SkillUnlocksPackTest on oak@15/cape@99/prayer-potion@38). **Combined-run site grouping (2026-07-16, Luke):** `groupBySite` pulls every stop sharing a SITE behind the first visit in `selectedRunLocations` — site = same location NAME and within `SAME_SITE_TILES` 50 (Catherby fruit↔herb is 47 = one walk; "Falador" park↔farm is 66 = two teleports, and names alone over-group), with "Farming Guild cave" a vouched alias of the guild (underground tile defeats any distance test); `farmGroupSites` toggle default on. Overlay hides the stop teleport within `NEAR_STOP_TILES` 50 same-plane ("nearby" — it told Luke to spirit-tree while he stood by the gnome fruit tree; the pack tiles were VERIFIED against the wiki's patch-page {{Map}} coords first, via `<page>/Patches?action=raw`). `RUNE_SUBSTITUTES`: combination runes satisfy base-rune teleport asks (dust=air+earth …). **Compost advance hears every wording (2026-07-16, Luke's Lletya fruit tree):** the vendored CompostTracker matches FOUR patterns — bucket use, the Fertile Soil spell ("The X has been treated with…"), already-treated, inspect — and Iron Hub's advance signal originally only matched the bucket one, so a spell-composted stop never advanced; `compostApplied()` now carries bucket + Fertile Soil + already-treated. **ShortestPathBridge posts on the CLIENT THREAD** (`clientThread.invoke` around `eventBus.post` — EventBus runs subscribers on the calling thread, and a sidebar Skip posted from the EDT left Shortest Path pathing to the previous stop until the next client-thread advance). **Supply warnings (2026-07-16, Luke):** `supplyWarnings()` under the Start-all button — ultracompost vs compostable stop count ("Ultracompost 2/9 — a Supercompost run makes more"; a FILLED bottomless bucket silences the check, charges unreadable, never invent a shortage; hespori takes no compost) + per-category "Herb seeds 2/4"/"Tree saplings 1/5" from the new pack `seeds` section (gen_farm_runs.py SEEDS, gameval-resolved; seeds warn but never cull — saplings still cull) in STATUS_WARNING red. **Teleport prefs heal across co-located ids**: prefs persist per location id, so a pref saved before a same-site sibling existed in the pack (gnome fruit tree arrived after the tree) silently missed it — `pickTeleport` now falls back to a same-name sibling's pref the location also offers, and `refreshStopTeleports()` re-resolves an ACTIVE run's stops when a pref changes (stops snapshot their teleport at startRun; the overlay showed yesterday's royal seed pod otherwise). **Hespori run (2026-07-16, Luke):** `hespori/farming-guild` curated at the vendored HESPORI region 5021 (the cave under the guild's west wing, skill:Farming:65 per the wiki, guild teleports), route "Hespori"; `categoryTab("hespori")` = Tab.HESPORI (a REAL tracked patch — cull-on-growing, harvestable "Ready" and overlay state all work, unlike birdhouse/contract/compost's null) and it is EXEMPT from the compost advance-gate (the patch takes no compost, planting is the finished work — safe because cull removed still-growing stops and the varbit only moves on a live visit); own "Hespori" setup bucket because the run is a boss fight wanting combat gear. Name + "Ready" go ColorScheme.PROGRESS_COMPLETE_COLOR when ready, else no right-hand text at all (the "N stops" count is gone) and NO row tooltip. **`runLocations(name)` now resolves custom runs and returns empty for an unknown name** — it used to fall through to `templateLocations(TEMPLATES.get(name))` = NPE, which every picker row would now hit (they all ask for locations to know ready/icon). ColorScheme (not UiTokens) for these status colours, to match Time Tracking's system; **bank title** overridden on BANKMAIN_FINISHBUILDING post-fire to the readable run name (Bank Tags shows the raw hidden tag otherwise; OPTION_HIDE_TAG_NAME only affects the right-click menu); **bank items still to withdraw glow green** reusing `com.loadoutlab.ui.BankHighlightOverlay` (new+overlayManager.add; WidgetItemOverlay gets deps via setOverlayManager on add) driven by `farmBankHighlight()` (setup items minus carried); **ready infoboxes** show green "Ready" text (reminder parity). **Post-M8: dailies rebuilt as a guided REPEATABLE-EVENTS run** (per user /goal, mirroring Farm runs): `data/dailies.json` (tools/gen_dailies.py — the OSRS wiki's Repeatable events page for scope/tiers/gating, joined to RuneLite core's **Daily Task Indicator** for detection, ported BSD-2 at runelite-parent-1.12.32, licenses/runelite-LICENSE). 10 events: NMZ herb boxes are dropped (the game blocks ironmen — core checks `IRONMAN == 0`) and the cape/ring charges + Zulrah/Fight-Cave resurrections are dropped per Luke (spent where you stand, so a run can't route to them). **Detection semantics are inverted from the obvious reading and are the thing to not 'fix': a `<X>_LAST_CLAIMED` varbit reads 0 while UNCLAIMED**; they only refresh on login, so crossing 00:00 UTC while logged in falls back to core's `dailyReset` assumption (`crossedReset()`); `MORYTANIA_SLIME_CLAIMED` COUNTS (cap = legs tier 13/26/39) rather than flags. `DailyTracker` is pure/static (AccountState + daily + crossedReset + now → AVAILABLE/DONE/LOCKED/UNKNOWN); tier ladders give the live quantity (best met tier wins) and scale the display-only `bring` line (120 staves = 840,000 coins). **Tears of Guthix has NO cooldown varbit** (5099 = in-minigame ticks-left, confirmed vs the Improved ToG Interface hub plugin; 451–456 are bitfields in varp 449) — but Juna's opt-in reminder ANNOUNCES it in chat ("You are eligible to drink from the Tears of Guthix", daily on login while eligible; wording corroborated by two independent server emulators, matched as a tag-stripped substring, carried in the pack as `detection.chat`). Signal order: reminder flag (`dailyeligible_<id>` unlock) → a visit we watched (`TOG_MINIGAME_COLLECTING` stamps it, starts the rolling 7 days and clears the flag) → UNKNOWN + "?". The reminder is opt-in so silence isn't evidence — the tooltip says to ask Juna. NOT the fixed Wednesday the old code assumed. Run engine mirrors farm runs: `cull()` (deselected/locked/claimed are not stops), advance ONLY on the claim varbit flipping (never on arrival), Skip = `markThrough` (and the only way past Miscellania, which has no claim flag at all — mode `manual`), End run pinned to the tab top, ShortestPathBridge routing, `DailiesRunOverlay` + two infoboxes (outstanding count green / run progress). Checklist = one `JCheckBox` per event persisted as the OFF set (`dailiesOff`, so new events opt in by default); row colour = state, tick = eligibility. **No teleport table** — Easy Farming gave farm runs audited teleports and no equivalent source exists here, so `travel` is the wiki's wording as a hint and Shortest Path picks. **Two routing targets deliberately aren't the NPC's tile** (pinned by DailiesPackTest): Lundail's Mage Arena bank can't be walked to (no lever transport in Shortest Path's transports.tsv) → target the surface lever; the Chasm of Tears is **plane 2** (the wiki {{Map}} omits the game plane; SP's swamp-cave tunnel lands (3219,9532,2)), and Advisor Ghrim is plane 1. Checklist rows are preceded by a **tile strip** (one per event, pack/route order = stable; `DailiesTab.DailyTile` — the farm overview tile MINUS click-to-expand: item sprite via ItemManager from the pack's `icon`, **GREEN = READY** (per Luke, farm-overview parity — ColorScheme.PROGRESS_COMPLETE_COLOR; NOT UiTokens' green-is-done), plain BORDER_BUTTON = tracked but nothing to do (claimed/unknown), and locked OR unticked = no border + dimmed fill ("not part of your dailies" — an amber/green "go do this" must never show for an event you excluded). The checklist text follows the same scale (`statusColor`): green claimable / TEXT_FAINT done / grey locked / muted+"?" unknown. Tooltips on tiles, checklist rows and run stops are **just the daily's name** — per Luke, the long where/bring/travel/note tooltip was too much — EXCEPT a SHORT row, where the missing items are the point. **Pack-driven per-event flags (2026-07-16):** `optOut` (Lundail — off until asked; persistence is now an explicit `dailiesChoice` map, since an OFF-set can't express "off by default") + `warning` (a JOptionPane on tick-ON only, and the wiki skull `data/icons/wilderness_skull.png` beside the name — the checkbox must be `setMaximumSize(getPreferredSize())` inside its row or BoxLayout shoves the skull to the far edge). `bring[].itemIds` present = SUPPLY-GATED: state SHORT (orange, culled from the run, tooltip lists what's missing) when bank+inv+worn < per x tier quantity. Robin's 34 bone types are curated from the wiki's **Bonemeal** table (the authoritative "which bones have a bonemeal" list; his page adds "bones do not need to be of the same type" + no Sun-kissed) and resolved via the item-name index — do NOT sweep constant names for BONES, that catches a herblore secondary, quest items and SMALL_KEY_ISLE_OF_BONES. Coins have no itemIds = told, not gated. Deselecting anything drops its green highlight (both modules). Icons cross-verified in DailiesPackTest against data/index/item-names.json (built from LEGACY ItemID constants = an independent source from the gameval names the generator resolves; catches picking BLANKRUNE over BLANKRUNE_HIGH). Renders: build/reports/dailies-{tab,run-active,run-overlay}.png; semantics in DOMAIN-NOTES "Repeatable events". **Post-M8: HUNTERS' RUMOURS module (2026-07-18, per user /goal — Dailies hub below Farm runs, count 22; Hunter Rumours plugin PORTED, BSD-2 geel9/DapperMickie, licenses/hunter-rumours-LICENSE):** `data/hunter-rumours.json` (tools/gen_hunter_rumours.py @ 4fcf4e8 — the Hunter/Rumour/Creature/Trap/RumourLocation enums parsed byte-faithful: 6 hunters+tiers, 29 rumours w/ trap + pity thresholds (bare + full-outfit, linearly interpolated per owned piece), per-creature Hunter-xp catch values (herbiboar = an xp RANGE 1950-2461, everyone else discrete drops — the catch-counting signal), assigning tiers, and 55 hunting-location areas deduped to one representative point each w/ fairy ring codes + spot counts; legacy NpcID/ItemID via javap; Hunter level = skillb: boostable). Built like the SLAYER module: HunterRumoursModule reads the current rumour from the burrows Hunter DIALOG (region 1549-1565,9449-9464 plane 0 — the plugin's own bounds) and the Quetzal whistle's "your current rumour target is …" GAMEMESSAGE (pack.rumourReferenced/hunterReferenced substring matchers); catches counted from Hunter StatChanged deltas matching the target creature's xp values (stop at the rare-piece message); records (PersistedState.RumourRecord capped 50: rumourId/hunterId/caught/pieceFound/duration) in IRON HUB's own persistence, NEVER the reference plugin's config group. Tab (HunterRumoursTab, chip views Rumour · History): hero card w/ target icon + catch meter vs pity rate, trap/level/assigning-hunter stat rows, outfit pity note, req gate, hunting locations w/ persisted preferred (click to toggle) + Shortest Path Route; History = record cards. Second pass same day (Luke's word — the parked client-side aids came in): NPC HIGHLIGHT via NpcOverlayService (id-equality on the target creature's npcId — no name patterns needed, unlike slayer), WORLD-MAP pins at every hunting area of the current target (painted marker, tooltip w/ fairy code, cleared/rebuilt on rumour change via clientThread), FAIRY-RING TRAVEL LOG highlight (WidgetLoaded/Closed InterfaceID.FAIRYRINGS + PostClientTick re-assert while open — the game re-renders the log continuously; '(Rumour) ' prefix + green on the preferred area's code in CONTENTS dynamic + FAVORITES static children, ONE UPDATE_SCROLLBAR runScript per panel lifetime — the reference's scrollbar-sprite spelunking was dead weight, the runScript IS the scroll), GEAR SETUPS in the slayer grammar but keyed PER TRAP TYPE ('Hunter: Net trap' — rumours sharing a trap share gear; the Loadout key space so both tabs interoperate; Show-in-bank arms a FarmBankLayout apply, title override on FINISHBUILDING), and HunterRumoursOverlay (hunterOverlay toggle: target + caught/pity, trap + level, preferred area + fairy code hidden within 30 tiles, carried-check missing trap item by PACK NAME — trapItemName field added since state.itemName only knows names it has seen — and the piece-found return banner). Toggles: hunterOverlay/hunterHighlight/hunterNavAids. Still parked: back-to-back dialog automation (the one true automation in the reference). Renders: build/reports/hunter-rumours-{tab,history}.png. **Post-M8: PoH module (2026-07-18, per user /goal — new module, count 21):** `data/poh.json` (tools/gen_poh.py — 13 useful spaces/39 tiers curated per the wiki's Guide:POH_Progression_&_Layout_Guide, soft-checked against the guide page at generation; every tier's hard data from its OWN furniture page's {{Infobox Construction}}: Construction level (emitted `skillb:` — POH building is the textbook boostable gate), room, icon itemid, and the BUILT furniture's `id` list — the wiki infobox carries ALL built-object state variants, killing constant-hunting; 15 tiers whose pages lack ids get curated gameval ObjectID constant names resolved via javap — pools RECOVERY/REGENERATION = fancy/ornate rejuvenation, chapel altars = the three god-variant ids per tier, POH_SPIRIT_RING = the combined tree-ring). Detection (PohModule): furniture object spawns gated on the game's "Welcome to your house." message (a friend's house never marks; spawns and the message RACE, so spawns buffer per scene load and commit on confirmation, then live while confirmed — building-mode swaps commit immediately); LOADING/LOGGED_IN resets. Manual mark per tier row = the escape hatch (POH layout is unreadable outside the house). Persisted `pohBuilt` tier-id set. Tab (PohTab): StoneTile grid (green bevel = ladder complete, orange = next tier buildable NOW via the graph, plain = progressing, dim = locked+unbuilt), single expansion (farm grammar) = the tier ladder w/ built green / next tier's needs: line / later faint, W wiki glyphs, click = manual toggle. Registered: Progression hub block, `pohProgression` toggle, module count 21. Render: build/reports/poh-tab.png. **Post-M8: QUESTS rebuilt (2026-07-18, per user /goal):** quest-cape hero chart (done/total quests + StoneProgressBar + QP · in-progress line — the cape is EVERY quest complete), completed hidden by default (small-font Show-completed toggle), chip splits Quests · Miniquests + sorts Difficulty · A-Z · Started; gen_quests.py grew per-quest `difficulty` (infobox param; RFD subquests = "Special") and full miniquest coverage (19 — the OQG route's plus the wiki Miniquests CATEGORY members that resolve to the Quest enum; the Miniquests PAGE is DPL-generated, its wikitext has no rows — use action=query list=categorymembers). Miniquest identity = pack flag, with enum-not-in-pack falling into the miniquest bucket EXCEPT the "Recipe for Disaster" parent (its pack identity is the ten subquests — curated exception in isMiniquest). Per-quest `+` = a CUSTOM goal (`custom:quest:<slug>`, req `quest:<name>`) — the engine expands the full prerequisite chain for free, no new goal family needed. Row click opens the quest in QUEST HELPER via reflection (PluginManager scan for com.questhelper.QuestHelperPlugin → questMenuHandler.startUpQuest on the client thread — QH exposes NO PluginMessage API at 9866465; verified their field/method names; inert when absent, wiki-page fallback; **HUB-REVIEW RISK flagged: replace with an upstream PluginMessage before any Hub submission**). W glyph keeps the wiki (mousePressed + e.consume so the row click doesn't double-fire). TEST LESSON (the doubled-rows bug's third appearance, new mechanism): RebuildGate queues HEADLESS rebuilds on the EDT via invokeLater — a test that mutates state then renders on the test thread races the QUEUED rebuild's removeAll(); mutate inside invokeAndWait AND drain with an empty invokeAndWait barrier before SwingRender. Renders: build/reports/quests-{tab,miniquests}.png. **Post-M8: CLUES & STASH rebuilt (2026-07-18, per user /goal; STASH Tracker ported — BSD-2, licenses/stash-tracker-LICENSE):** `data/clue-steps.json` (tools/gen_clue_steps.py — core's cluescrolls EmoteClue table at the client tag: 126 emote steps with tier/text/point/STASH link and the item-requirement DSL parsed to graph-string lists with DISPLAY NAMES in the item: leaves (`item:<id>:1:<Name>` — raw ids on needs: lines read as garbage); ItemVariationMapping streams collapse to single item: leaves (the graph counts variants); emptySlot = the `nothing` flag which COEXISTS with item reqs ("no jewelry"); + STASH Tracker's 119-unit table @ a8caedc: tier/display name/HH_* built-object ids resolved across the SPLIT gameval ObjectID+ObjectID1 classes; four core-vs-mirror constant drifts aliased, Warriors' Guild banks disambiguated by object id). Old 7-clue clue-items.json/ClueItemsPack DELETED. Detection (ClueStashModule): built = the HH object spawning (only renders for its builder), filled = keyword-loose stash chat attributed to the clicked unit within 5s else nearest within 5 tiles, manual toggle in the tab = the ESCAPE HATCH for pre-plugin fills (no varbit exists — see DOMAIN-NOTES "STASH units & emote clue steps"); persisted stashBuilt/stashFilled object-id sets; filling implies built, emptying doesn't unbuild. Ready-to-fill = unfilled unit whose linked clue's reqs are met (bank+carried — POH costume storage unreadable, the tab says so). Clue-step goals: `+ Goal` on blocked steps → `clue:<id>` synthetic goal (seed in `clueGoals` w/ text/tier/reqs; toClueGoal steps = the item reqs; proof = `cluestep_<id>` unlock the module marks the moment reqs are met, incl. at add time). Tab (CluesTab, chip views Steps · STASH): per-tier doable counts, blocked-first rows w/ needs: + goal buttons, show-doable toggle, 15-row caps; STASH summary card (built · filled · sets-owned-not-stored), per-tier built/filled counts, status-coloured unit rows (click = manual filled toggle) w/ ready badges, show-filled toggle. Clue COLLECTION LOG deliberately absent (Luke: that's the future Collection log module's). Renders: build/reports/clues-{steps,stash}.png. **Post-M8: the SLAYER SUITE (2026-07-18, per user /goal — "the best Slayer tool ever put into RuneLite"; five reference plugins analyzed at pinned commits: Slayer Simplified, Turael Skipping, Configurable Slayer Task Overlay, Slayer Task Sorter, Better Monster Examine — ported, not imported; BSD-2 licenses + README credits):** `data/slayer-tasks.json` (tools/gen_slayer.py, six pinned sources — core Task.java @ the client tag for canonical names/target-name matching/finisher thresholds, the wiki's nine per-master assignment tables parsed to requirement-graph string LISTS (allOf semantics — top-level `&` is NOT graph syntax, the pack test proved it), the wiki Bucket API's monster stats fetched at BUILD time, Slayer Simplified's locations/coords/access-reqs/bring lists with metal-dragon variant files folded into Metal dragons, Turael Skipping's 24-task registry byte-faithful incl. the goblin factory reconstruction, and the Slayer Rewards page joined to gameval VarbitID constants — reward names are PUNS, never guess them, fail-fast on unmapped rows). Requirement graph grew a `combat:<level>` leaf (Experience.getCombatLevel over the seven skills). Detection (SlayerOptimizerModule, the suite brain): gameval varps + DBTables exactly as core's Slayer plugin (boss sentinel 98 via SlayerTaskSublist, Konar area via SlayerArea, SLAYER_MASTER focus ids = the block-list clientscripts' enum, decode in DOMAIN-NOTES "Slayer detection & block lists"); task RECORDS (PersistedState.SlayerTaskRecord capped 50: master/assigned/kills/xp/loot value/duration; kills from count decrements EXCEPT drops-to-0 > 2 which are points skips; completed ONLY on an observed streak-varbit increment — wilderness twin summed for Krystilia; loot from NpcLootReceived matched via target patterns); NPC highlighting through core's NpcOverlayService (pack targets + de-pluralized name, NBSP normalize, Attack/Pick gate — pure matchers unit-tested); per-master block lists read LIVE from the game's own varbits (SLAYER_BLOCKED_<MASTER>_1..6+DIARY, Spria shares Turael), ids resolved via DBTable with honest "Task #N" until resolved; unlock varbits mirrored into `unlock:slayerreward_*` graph flags. Tab (SlayerTab, chip views Task · History · Unlocks · Blocks): hero card w/ kill meter + master/area, points/streak, live record line, Turael-skip section, req-gated locations with persisted preferred (click to toggle) + Shortest Path Route buttons, bring list w/ variation-aware ownership colours, finisher hint, bundled monster stats, per-task saved gear setup in the LOADOUT's key space (state.savedSetup(taskName) — the two tabs interoperate for free) with "Show in bank" arming a FarmBankLayout apply on the next bank open (user-initiated, never automatic — a task is active 95% of playtime), per-task note that survives state-driven rebuilds; History = newest-first record cards (done green / skipped amber / active orange); Unlocks = the 53-reward catalog vs live varbits; Blocks = master selector + live slots + persisted block/skip pref lists (add-combo sorted heaviest-weight-first — the Slayer Task Sorter feature reborn) + point-cost advice. SlayerSuiteOverlay (slayerOverlay toggle): task/remaining/progress/master·area/live stats, superior flash (10s), Turael spot + capped teleports auto-hidden once standing in the kill area, preferred location, carried-check missing REQUIRED items in red (bank doesn't count — new AccountState.carriedCount), skip-list advisory. Advice in chat (slayerBlockAdvice toggle, CONSOLE via ChatMessageManager): on assignment when the task is on its master's skip list, and on visiting a master (ChatLeft dialog name, edge-triggered per visit) listing preferred blocks not yet made — SILENT while any live slot id is unresolved, never advise on a guess. StateFixture grew varbitChanged/varpChanged (the live onVarbitChanged delivery path — silent seeds never notified listeners, which change-driven module tests need). Renders: build/reports/slayer-{task,history,unlocks,blocks,overlay}.png. Parked: STASH-style per-task world map pins, monster drop tables (the loot module + wiki links cover it), tile-note safespots. **Birdhouse run (2026-07-16):** the Bird_house_trapping wiki route (Verdant Valley N→S, Mushroom Meadow N→S on Fossil Island; Digsite pendant + mushtree; gated Bone Voyage + Hunter) as a curated route — new `birdhouse` category with NO PatchImplementation (bird houses are the vendored hunter tracker, not farming patches; FarmRunsPackTest skips their region check; categoryTab=null → manual markThrough like Supercompost); tiles approximate (mushtree navigation, not walking).
- `com.ironhub.ui.IronHubPanel` — the panel shell; **home is `HomePanel` since the 2026-07-16 nav rework** (OSRS-skinned, follows `osrsTheme` via `IronHubPanel.themeChanged()` from the plugin's ConfigChanged): player name (placeholder "Iron Hub" until wired), live Combat/Total/XP summary from AccountState (`Experience.getCombatLevel`), then a Progress-to-next-goal box (2-line label + a StoneProgressBar where a stat value would sit — SAMPLE 31% until wired) beside Achievements Completed (SAMPLE 397/492), then SIX nav blocks (round 3: Luke cut Current task so nothing squashes, and the row became the Design lab's — full 33×36 stones, FLUSH, icons at NATIVE size never rescaled, since LANCZOS smoothing made round 2 read soft) — all six icons are Luke's own drops (2026-07-17 "new icons for nav bar" set, native size, never rescaled): Goals = quest scroll with ?, Combat = crossed swords, Dailies = clock, Progression = treasure chest, Bank = $ coin, Settings = gear — icons under `data/icons/osrs/nav/` (`OsrsIcons` falls back mystic→vanilla exactly as a resource pack does in-game), **wired 2026-07-17 (round 2, Luke's alignment list)**: the home view is ONE bare-scroll surface (HubScrollPane(showBar=false) — the visible bar narrowed content and misaligned it under the header; wheel still scrolls) with the open block's hub page mounted via `HomePanel.contentSlot()` BELOW the stone frame, directly on the theme backing — since 2026-07-17 (Luke's hierarchy call) the raised frame envelops only the hub proper (title, summary, stones) and ends above the module header plates; home, scroll pane and card are all theme-backed (no classic grey behind the skin), one connected scroll where only the content changes. Stones toggle-select with a lifted bevel. **ALL SIX blocks wired + classic nav DELETED (2026-07-17, Luke's word)**: `IronHubPanel.BLOCKS` = Goals→Goal planner; Combat→Loadout+Slayer+Loot & supplies; Dailies→Dailies+Farm runs (renamed 2026-07-17 from Loadout Lab / Dailies (New) / Farming runs — module name() values must be UNIQUE, the panel maps by name: the classic dailies brain is now "Dailies (classic)", tab-less). **Loadout rework (5973c3d): live gear+inventory view on TOP (fingerprint-gated over captureSetup+pouch), Save/View-all-setups buttons, inline setups list, setup-vs-current diff (ORANGE swap / RED deposit-only, SetupDiff truth table), bank collects the withdraw list while viewing (BankCollectView public in ui.components, per-owner tag names), 3/4 pouch slots by carried pouch id, spell/style line = honest NUMBERS (no API maps autocast varbit 276 to a name — verified), slot click → ItemNameIndex search → copy-on-write draft.** Progression→Collection log+CA+Gear progression+Diaries+Quests+Clues & STASH+QoL; Bank→Bank & banked XP+Supplies runway+Death recovery; Settings→Design lab (Loot/Design-lab placements were forced homes once the classic nav went; External sync is tab-less; the classic Dailies TAB left the nav — the module keeps the brain the pilot renders). The Modules button, `ModuleNavPanel`, the per-module cards and the CardLayout are GONE — the stones are the whole navigation; NavHeader survives only as a classic atom (AtomGallery). Each stacked module gets a **stone header plate** in the hub page (`IronHubPanel.moduleHeader` — notched box, title in the BOLD game font + TITLE orange per Luke; with the frame ending above them the plates carry the section hierarchy alone), so `DailiesNewTab`'s own title was cut — migrated tabs must NOT carry their own title, the host plate names them. **Since 2026-07-17 evening the plates are COLLAPSE TOGGLES and hub sections are EXCLUSIVE** (Luke, after continued dev-client crashes moving between modules): only ONE module's tab per hub is built and mounted at a time (`expandedModules` map, first module open by default, choice survives theme swaps, triangle affordance + press-anywhere; collapsed slots are empty so RebuildGate never fires for them) — building a seven-tab Progression page on one click was the resource spike. Skinned tabs are FRAMELESS (content on theme.background; the host provides frame + header plate): as of 2026-07-17 EVERY module tab wears the skin — Dailies (New), Farm runs, Goal planner (+GoalsTab), Loadout Lab wrapper, then the twelve-port sweep: Quests, QoL, Clues & STASH, Supplies runway, Slayer, Death recovery, Loot & supplies, Bank & banked XP, Diaries, Gear progression, Collection log, Combat achievements. NO clone surfaces remain — the farm TimeablePanel rows, the last one, went to stone on Luke's word 2026-07-17 evening (2026-07-17 second pass per Luke: the Loadout Lab PANEL INTERIOR and the Inventory-Setups slot grids are stone too; the panel styles itself at construction from LoadoutLabPanel.setIronHubTheme and the module RESTARTS the lab on a theme flip so a fresh panel re-clothes; the Saved setup renders as the GAME's interfaces via SavedSetupView — measured Worn-Equipment slot layout + chain links, ink-centred pouch runes with separately painted counts, inventory on the per-theme bottom_line_mode_side_panel frame with measured strip bands and WHOLE corner pieces; boost/spellbook toggles on slot_tile/slot_selected slabs; MYSTIC.background = Luke's #282828; the full vanilla+Mystic sprite dump lives at design/all_sprites — check it first for art, and NEVER put reference sprites under src/main/resources, which ships in the jar). Loot & supplies lists are flat Route-grammar rows (16px sprites, MUTED text incl. counts — Luke: light text, not green; stack counts never baked into 16px sprites). Custom-painted status components translated to stone while keeping semantics: diary tier bars (BAR_TROUGH + VALUE/TITLE darkened fills), gear ItemTile + CA boss tiles (StoneTile bevel grammar). UiTokens/classic atoms now serve NO live tab — only the unmounted DashboardPanel and shared non-tab chrome. Skinned modules re-clothe on an osrsTheme flip by DROPPING their cached tab in onConfigChanged (invokeLater) — themeChanged has already closed the open block, and the panel's next mount builds a fresh tab in the new theme (FarmingRunModule, GoalPlannerModule — EventBus now injected — and LoadoutLabModule all follow it). Hub pages mount module tabs via **mount-on-show**: tabs are singletons with one Swing parent, so a hub page adopts the EXPANDED module's tab when shown (`IronHubPanel.mount` via `refreshHub`); a theme swap drops the cached pages and the fresh slots re-adopt the open module (NavBlocksTest pins it); `invalidateModule` re-mounts a toggled module's slot only where it is the expanded one (off → "Enable the … module" note, on → the live tab). Guards: ModuleLifecycleTest proves every BLOCKS name resolves to a module; NavBlocksTest proves every NAV stone has a hub page; renders home-dailies-hub.png + home-progression-hub.png (ModuleRoutingTest). **WhatNow and the old Loadout module were DELETED in the same rework** (Luke's word): `modules.suggest` gone (incl. its DashboardPanel card + the planner's whatNow test), `modules.loadout` keeps only StrategyClient/WikiStrategy/DpsExport (Loadout Lab uses them); the old classic `DashboardPanel` (frame 1b) is no longer mounted but stays compiled + render-tested pending the rest of the rework. Renders: build/reports/home-{stone,mystic}.png. `com.ironhub.ui.components` — shared atoms (M1). `com.ironhub.ui.UiTokens` — all design tokens as constants (mirrors DESIGN-PACKAGE.md; keep them in sync, never hardcode styles in views). Offscreen renders for mockup side-by-sides: `./gradlew test` → `build/reports/*.png`.
- `com.ironhub.ui.osrs` + `com.ironhub.modules.designlab` — the OSRS "stonework" skin, DEVELOPMENT STAGE (design/OSRS-SKIN.md is the spec; per user direction 2026-07-16, aiming to make the sidebar read as an extension of the game). Tokens/atoms sampled pixel-exactly from the wiki's native-1x `File:Character_Summary.png` (never eyeballed): `OsrsSkin` palette + FontManager RuneScape fonts, `StoneBorder` (engraved 1px dark/light edge pair + a stepped concave corner stamp, mirrored ×4), `StonePanel`, `OsrsLabel` (+1,+1 black shadow, antialiasing OFF, the game's 12px line pitch — NOT FontMetrics height, which runs 4px/line too tall), `StatBox`. THEMED via the `OsrsTheme` ENUM (surface colors + corner stamp; text colors/fonts stay global on OsrsSkin — packs re-sprite art, never text): `OsrsTheme.STONE` (vanilla, 7×7 stamp) and `OsrsTheme.MYSTIC` (Luke's daily Resource Packs skin, 6×6 stamp — sampled from the pack's own sprites at pinned commit 339ac716; BSD-2, licenses/mystic-pack-LICENSE + README credit; its `quests_tab/` icon redraws bundled under `data/icons/osrs/mystic/`). Both themes dress EVERY atom; the live one is the `osrsTheme` CONFIG setting (plugin settings, never a sidebar control — Luke's call). Phase-2 atoms: `StoneFrame` (thin resizable-mode side-panel frame — 1px dark/light + 8×8 stepped chamfer, geometry from the pack's `tab/side_border_*` 9-slice = the game's own SIDE_PANEL grid), `StoneButton` (**the default button** = the notched box + hover/press fills), `StoneNavButton` (square stone 33×36, **navigation only** per Luke, carrying the game's FULL-SIZE tab icons via `OsrsIcons.tab(theme,…)` — the 18px summary icons read as toys at that weight; vanilla icons are the wiki files the game's own Interface page names, Mystic's are the pack's redraws), `StoneProgressBar` (18px, left/center/right labels inside in the game's SMALL font — layout from RuneLite's own ProgressBar per Luke's reference; **fill is the caller's semantic colour** and the trough is the theme-independent `OsrsSkin.BAR_TROUGH` #3E3830 per Luke, since each theme's recess swallowed the bar), `StoneMeter` (5px, no text), `StoneScrollBarUI`/`StoneTextField`/`StoneComboBoxUI` (phase 3 — they skin the REAL Swing components, since 5 modules already use JComboBox and several JTextField, so migration is a styling swap; sampled from the Settings interface + the pack's scrollbar sprites/overrides.toml; `fieldEdge` is its own token because a field is a sunken WELL, not an engraved box — Mystic's field interior IS its edgeDark, so box tokens left it borderless; typed text = MUTED per Luke with placeholders in the derived FAINT; **any SWING-drawn text needs `OsrsSkin.crisp(c)`** — the Graphics AA hint is overridden by the LAF's own, which smears the pixel font with LCD subpixel fringes, and only the per-component client property wins — **and +1px top padding**, since Swing centres by the font's EM BOX while this font's ink floats high in it, so em-centred controls read 1-2px high; placeholders draw at the field's own getBaseline so they land where real text will), `StoneCheckbox` (15px — ODD so it centers exactly in the odd row height the font's ink demands; tick centered from its own measured bounds)/`StoneChecklist` (rows grouped in ONE notched frame, highlight is a BAND inset EQUALLY on all four sides (the corner-stamp height, so sides match the top-row spacing), wrapping the checkbox with a 1px gap every side — Luke walked this to the pixel across three rounds (stat-box padding = ugly gap, full-bleed = too far, 1px sides = closer to the sides than the top), rows hand-laid-out since BoxLayout's aligned-span rounding put the box a pixel high). A `StoneTabStrip` was built beside the nav stones so Luke could judge both; he picked the stones and it was DELETED (git history has it). The lab keeps BOTH nav icon weights (large tab icons + the small summary set) stacked, per Luke, so they stay comparable. Ground truth beyond the summary screenshot: `File:Settings_interface.png` (vanilla checkbox/button/tabs/scrollbar at 1x) — note the game has TWO button idioms (Settings = raised black-outline bevel; stat boxes = engraved groove; we use the engraved one). The game has no pointer, so hover/press are the ONLY derived tokens (Mystic's hover is sampled from its own `_hovered` sprite, flattened from a gradient). Icons: wiki `Character_Summary_-_*` files + two spirals cropped 1:1, under `data/icons/osrs/`. "Design lab" nav row (Development category, `designLab` toggle) renders every atom in the active theme behind an Atoms · Goals hub chip switcher — the second view is `GoalsHubMockup`, the **Goals v2 single-surface interface proposal, round 2** (design/GOALS-V2.md §0.1+§6, sample data; Luke's round-2 verdict reshaped it: Goals/Tasks/Routes vocabulary, benefits-lead-the-copy-not-hours, FOUR sections — hero w/ click-through Done-this-month, CURRENT TASK card w/ progress bar + number-left stats + Why line, GOALS = category-grouped expandable Route rows w/ per-task arrows+pin, SUGGESTIONS w/ benefit-kind cards incl. merge offers; NOW/budget-chips/ROUTE/COMPLETED sections all CUT); renders = build/reports/designlab-{stone,mystic}.png + goals-hub-{stone,mystic}.png (side-by-side vs the source art is the acceptance test; DesignLabRenderTest pins both themes' border anatomy, token completeness, button states, single-pass label heights and ink centering). Later atoms from the module ports (2026-07-17): `StoneChipRow` (the skin's segmented control — selected chip = selectFill + TITLE text, stretch mode divides the width evenly; planner views/budget/layout switches), `OsrsLabel.wrapped()` (FontMetrics greedy word wrap — html measurement and the pixel font DISAGREE: the html view lays lines out with its parse-time font and clips mid-word, so skinned wrapping text never uses html), `OsrsLabel.squeezable()` (explicit min width 0, min HEIGHT stays honest) + paint-time ellipsis on any line wider than the assigned width (hub-hosted tabs are narrower than 225), `StoneButton.labelColor()` (saved-green etc.), `OsrsSkin.PROGRESS_BLUE` (goal/xp bars). TWO HARD RULES from the planner port: skinned text ALWAYS paints through OsrsLabel — a raw JLabel positions the pixel font by its wrong FontMetrics and clips glyph bottoms ("Dragon defender" read "Uraaon defender"); icons ride in a separate holder JLabel beside the OsrsLabel. And OsrsLabel.leftAligned() claims LEFT_ALIGNMENT — one centre-aligned child in a vertical BoxLayout drifts every sibling. Upgrade paths + fidelity verdict in the doc (`SpriteID.V2StoneBorders`/`Combatboxes` 9-slice sprites exist for runtime-perfect art). NO existing module migrates until Luke signs off; UiTokens remains authoritative for classic surfaces, and this skin deliberately breaks DESIGN-PACKAGE's no-shadow/radius rules inside skinned surfaces only. **Phase-4 pilot: "Dailies (New)"** (`DailiesNewModule` + `DailiesNewTab` in modules.dailies, `dailiesNew` toggle, count 22) — the dailies tab in the skin BESIDE the classic one; it owns NO brain (DailiesModule keeps detection/run/overlays; the skinned tab uses the same package-private seam + a `tabListeners` hook on rebuildTab so run events reach both tabs), follows osrsTheme via ConfigChanged, and dies when Luke approves the swap (then DailiesNewTab replaces DailiesTab). New atoms for it: `StoneTile` (status tile; dim = sunken recess + ghosted icon) and the richer `StoneChecklist.row` overload (status colour/tooltip/badge/callback). **Tiles wear the nav slab since 2026-07-17 (Luke's call, superseding the earlier flat-rectangle rule):** `StoneNavButton.paintSlab`/`paintSilhouette` are the shared chamfered-slab painters, `StoneTile` and the farm `OverviewTile` both use them keeping their status bevel colours, and perimeter-progress traces follow `StoneNavButton.ringPath` so the orange line hugs the chamfer instead of cutting the notched corners. Renders: dailies-new-{stone,mystic,run}.png.
- `com.ironhub.integrations.ShortestPathBridge` — PluginMessage soft integration, done in principle.
- `src/main/resources/data/*.json` — the data packs (dailies, banked-xp, qol, boosts, gear-ladders, gear-progression, goals, scenarios, quests, methods, effects, diaries, ca-completion, clue-items, xp-actions, clog, farm-runs, index/item-names), each with a schema under `data/schemas/` enforced by DataPackTest; most are generated by a `tools/gen_*.py` sibling — never hand-edit generated JSON, rerun the generator. Requirement strings use the graph's `parse()` form.
- Build: Gradle, Java 11, `net.runelite:client` latest.release. Dev launcher: `src/test/java/com/ironhub/IronHubPluginTest`.

## Implementation order (historical — M0–M8 are complete)

**DEVELOPMENT-PLAN.md was the authoritative sequencing** — milestones M0–M8 with exit criteria, all shipped (v1.0 reached, then several post-M8 rebuild arcs; see "Current code state"). What remains: the skills module stub (unblocked — methods.json exists), the boat module (parked until Sailing content stabilises), and whatever the user directs next — typically via `/goal`, usually one module or rebuild per arc. New arcs follow "The Iron Hub way" above, sliced into green commits. **NEXT ARC (in flight since 2026-07-18): Goals v2** — design/GOALS-V2.md is the agreed outline; **design/GOALS-V2-PLAN.md is the per-slice implementation handoff for G2–G8 (read it FIRST before any Goals v2 work)** — it carries scope, exit criteria, pitfalls and the two approval checkpoints (G4's regenerated methods pack and G7's deletion of the old planner views are presented to Luke BEFORE landing). G0 mockup approved at round 2 ("this is the right shape"); G1 (GoalSeed unification), G2 (PoH/QoL/Bank/Supplies providers + completion archive — see "track this" above), G3 (clog rates into the cost model), G4 (methods.json machinery + freshness) all LANDED. **G4 (gated — Luke chose KEEP PRACTICAL RATES over adopting WOM max-efficiency EHP, which would have made every estimate 30-165% faster/tick-perfect): rates UNCHANGED; gen_methods.py now fetches WOM's ironman EHP at a PINNED commit (1afcf8c, cache tools/.cache-methods/) and fail-fast CROSS-VALIDATES every curated rate ≤ the WOM max-efficiency peak × 1.5 (a rate-typo tripwire; WOM is the ENVELOPE, never the plan rate; Sailing has no WOM config, skipped). `MethodsPack.metaLabel()`/`isStale(nowMs)` (STALE_AFTER_MONTHS=4) surface "meta: Jul 2026 · rates may be out of date" faint under the planner horizon. MethodsPackTest pins the floor-method invariant (every ladder has a startXp≤0 method EXCEPT Herblore, quest-gated to level 3 by Druidic Ritual), req parse, freshness (noon-UTC anchored). **G3: `engine.RateSource` (built into EnginePacks from clog.json) fills the KILL/OBTAIN NaN gap — `CostModel.completionsToHours`/`dropHours`/`dropSpreadHours` are the shared geometric core ClogRanker now delegates to (attempts = MEAN completions = 1/p, one convention); Router.duration costs KILL by clog-activity-name match and OBTAIN by clog drop rate (gear curated `hours` stay PRIMARY, clog fills the gap — clog-only slots + bare `item:` leaves); clog goals' obtain step routes through `item:<id>` (a costable OBTAIN node) while the `clogitem_` unlock stays the achieved proof; `Plan.Step.spreadHours` = P90 "unlucky" tail (NEVER in the fingerprint), shown in the explain card as "up to ~Xh if unlucky"; RateSourceTest cross-checks gear-vs-clog hours agree within 10× (data-bug tripwire). Next: G4 (methods.json v2 generator — GATED, present the data diff to Luke before landing). Key decisions locked: **Goals/Tasks/Routes vocabulary** (Route = a Goal + its ordered Tasks; module `+` creates a Route; a Task can be another Route's Goal) and **benefits-first presentation — time is one stat, never every row's headline** (Luke: players act for stats/QoL/interesting unlocks, not pure time-efficiency); supply goals = one-shot "stock N × item"; iron-first (account-type varbit + UIM banked-xp honesty in scope, normal accounts later, opt-in); priority = tiers + Route pins + per-Route manual task order (arrows+pin, feasible positions only); ONE cohesive surface under the Goals nav block, four sections (hero / CURRENT TASK / GOALS as category-grouped Routes / SUGGESTIONS incl. merge offers; completed archive behind Done-this-month, depth 2) — **PlannerTab's Today | Route | Goals views are to be REMOVED** (G7); meta regenerations gated on prior proposal to Luke.

## Hard constraints (violating any of these = rework)

- Panel content exactly **225 px wide** (`UiTokens.PANEL_WIDTH`), vertical scroll only. Content that doesn't fit gets cut, tooltipped, or two-lined — never widened.
- Swing: flat fills, 1 px borders, radius ≤ 2 px, no gradients/shadows/animation.
- Status colors are semantic and never repurposed; accent `#DC8A00` is interaction-only and is NOT the amber status color (`#E0A23C`). See UiTokens.
- Navigation depth ≤ 2; back arrow + title on every non-home screen.
- Overlays ≤ 250×200 px, display-only. Infoboxes: 32×32 icon + short yellow text + tooltip.
- No client automation; Hub rules: no dependencies on other hub plugins (PluginMessage only), external HTTP opt-in.
- Heavy computation off the client thread; re-render on relevant events only, never per-tick.

## Conventions

- Modules stay self-contained: own package, own panel tab, own overlays/persistence; shared reads via `AccountState`, shared prerequisites via the requirement graph.
- Static game content (ladders, orders, methods, rates, clue items) is data-pack JSON under `src/main/resources/data/` — schema-validate additions; no gameplay constants in Java.
- Persistence profile-scoped: small flags via `ConfigManager`, bulk state as JSON under `RUNELITE_DIR/iron-hub/<profile>/`.
- Icons from the game sprite cache at runtime (the mockups' two-letter squares mark each sprite site). Status glyphs (✓ ● ○ !) are painted icons, not font emoji.
- ⊞/☰ view preference persists per module (profile-scoped config key).
- Code style follows RuneLite conventions (tabs, braces on new lines), Lombok available.

## Verification

- `./gradlew build` must pass (runs every test including schema validation and the parity
  sweeps); `IronHubPluginTest` launches a dev client for in-client smoke tests — the user
  usually does the live pass and reports back, so unit-prove everything you can headless.
- UI work: render PNGs from tests, inspect them, and compare against the mockup frame ids;
  tokens must come from `UiTokens`.
- Data-pack changes: rerun the generator (never hand-edit), and keep the pack's integrity
  test honest (parse checks, cross-references, count floors).
